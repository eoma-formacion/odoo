<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit event form view to add event banner field -->
    <record id="view_event_form_inherit_banner" model="ir.ui.view">
        <field name="name">event.event.form.inherit.banner</field>
        <field name="model">event.event</field>
        <field name="inherit_id" ref="event.view_event_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='badge_image']" position="after">
                <field name="event_banner" widget="image"/>
            </xpath>
        </field>
    </record>

    <!-- Add salesperson field to event registration list view -->
    <record id="view_event_registration_tree_inherit_salesperson" model="ir.ui.view">
        <field name="name">event.registration.list.inherit.salesperson</field>
        <field name="model">event.registration</field>
        <field name="inherit_id" ref="event.view_event_registration_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='sale_order_id']" position="after">
                <field name="salesperson_id" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>